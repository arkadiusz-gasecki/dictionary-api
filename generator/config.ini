[DATABASE]
; accepted values: sqlite, mysql+pymysql, postgresql

; example entries
Engine = sqlite
User = 
Password = 
URL = /home/user/Documents/dict_api/data.db
Port = 
Schema =

; Engine = mysql+pymysql
; User = root
; Password = root
; URL = localhost
; Port = 3306
; Schema = tmp

; Engine = postgresql
; User = root
; Password = root
; URL = localhost
; Port = 5432
; Schema = tmp

[TABLE]
Table_To_Parse = test_table

; do not modify entries below
; also, longer string before shorter, e.g. "INTEGER UNSIGNED" and then "INTEGER"
[MYSQL+PYMYSQL TYPES]
SMALLINT = SmallInteger, int
INTEGER UNSIGNED = Integer, int
INTEGER = Integer, int
DECIMAL = Numeric, float
FLOAT = Float, float
DATETIME = DateTime, str
DATE = Date, str
TIMESTAMP = DateTime, str
VARCHAR = String, str
CHAR = String, str

[POSTGRESQL TYPES]
SMALLINT = SmallInteger, int
INTEGER = Integer, int
DECIMAL = Numeric, float
NUMERIC = Numeric, float
FLOAT = Float, float
DOUBLE PRECISION = Float, float
DATE = Date, str
TIMESTAMP(0) WITHOUT TIME ZONE = DateTime, str
TIMESTAMP = DateTime, str
VARCHAR = String, str
CHAR = String, str

[SQLITE TYPES]
BOOLEAN = Boolean, bool
SMALLINT = SmallInteger, int
INTEGER = Integer, int
DECIMAL = Numeric, float
FLOAT = Float, float
CHARACTER = String, str
VARCHAR = String, str
CHAR = String, str

